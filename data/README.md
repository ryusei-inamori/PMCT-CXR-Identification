# Data templates

No clinical data are included.

Use `data/examples/queries_example.csv` and `data/examples/gallery_example.csv` as schema templates only. Replace every path and identifier with data approved for your environment.
